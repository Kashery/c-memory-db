#include <stdlib.h>


struct Node
{
    void* e_chain;
    struct Node* e_next;
    /* data */
};

int spawnNode(void* element)
{
    struct Node* head = NULL;
    head = (struct Node*)malloc(sizeof(struct Node));

    &ptr.e_chain = element;

}
int main(int argc, char const *argv[])
{
    
    return 0;
}
